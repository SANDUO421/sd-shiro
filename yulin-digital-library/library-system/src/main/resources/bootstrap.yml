NACOS_SERVER: 127.0.0.1
spring:
  application:
    name: library-system
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER}:8848
      config:
        server-addr: ${NACOS_SERVER}:8848
        file-extension: yaml
